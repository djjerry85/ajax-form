(function ($) {
    $.fn.sAjaxForm = function (options) {
        let defaults = {
                url: '',
            },
            settings = $.extend(defaults, options);

        return this.each(function () {

        });
    };
})(jQuery);